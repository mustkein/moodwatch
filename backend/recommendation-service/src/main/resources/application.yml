server:
  port: ${RECOMMENDATION_SERVICE_PORT:8084}

spring:
  application:
    name: recommendation-service
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: 6379
      password: ${REDIS_PASSWORD:}

groq:
  api-key: ${GROQ_API_KEY:}
  base-url: https://api.groq.com/openai/v1
  model: llama-3.1-8b-instant
